# AI 비서를 직접 만들어봤다 — GitHub Copilot SDK + 텔레그램

## 지난 이야기

[이전 글](https://wrestling-with-ai.blogspot.com/2026/03/botfather-claude-code-windows-ai.html)에서 Claude Code와 텔레그램을 연결해서 원격 머신과 대화하는 방법을 소개했다. 스마트폰에서 텔레그램으로 메시지를 보내면, 원격 PC에 있는 AI가 답변을 해주는 구조였다.

잘 동작했지만, 좀 아쉬운 점이 있었다. 그냥 CLI 명령어를 2초마다 반복 호출하는 단순한 방식이라, "이걸 좀 더 똑똑하게 만들 수 없을까?" 싶었다.

그러다 **GitHub Copilot SDK**라는 걸 발견했다. GitHub이 공개한 공식 개발 키트인데, 이걸 쓰면 Copilot의 AI 기능을 내 앱에 그대로 가져다 쓸 수 있다. 그래서 이번에는 이걸로 **제대로 된 AI 비서**를 만들어 봤다.

## 뭘 만들었나?

한마디로, **스마트폰 텔레그램에서 대화하면 내 PC의 AI가 대답하는 비서**다.

이전 버전과 다른 점은:

- 🧠 **더 똑똑해졌다** — 단순 질의응답이 아니라, 웹 검색도 하고, 할 일도 관리하고, 유튜브 영상 요약도 해준다
- 💬 **대화가 자연스럽다** — 이전 대화를 기억하고 이어서 대화할 수 있다
- 🔧 **기능 확장이 쉽다** — 새로운 도구를 추가하면 AI가 알아서 적절할 때 사용한다

## 어떻게 만들었나?

놀랍게도, **대부분의 코드를 AI가 짜줬다.**

### 1단계: 조사를 AI에게 시켰다

먼저 Copilot에게 "GitHub Copilot SDK가 뭔지 조사해줘"라고 했다. Copilot이 알아서 GitHub 저장소를 탐색하고, 문서를 읽고, 소스 코드를 분석해서 **41개의 참고 출처가 달린 기술 보고서**를 작성해줬다. SDK가 어떤 구조이고, 어떤 언어를 지원하고, 어떻게 쓰는 건지 전부 정리해줬다.

내가 한 건? "조사해줘"라고 한 것뿐이다.
```bash
/research github/copilot-sdk
```

### 2단계: 프로젝트 생성도 AI가 했다

조사 결과를 바탕으로, "이 SDK랑 텔레그램을 연동해서 개인 비서 만들어줘"라고 했다. Copilot이 어떤 구조로 만들지 계획을 세우고, 어떤 기능을 넣을지 물어보더니, 승인하니까 **프로젝트 전체를 생성**해줬다.

파일 10개 이상, 코드 수백 줄을 알아서 만들었다. 중간에 빌드 에러가 나면 스스로 고쳤고, 라이브러리 호환성 문제도 알아서 해결했다.

### 3단계: 텔레그램 봇 설정

이 부분만 내가 직접 했다. 근데 어렵지 않다:

1. 텔레그램에서 **@BotFather** 검색
2. `/newbot` 입력
3. 봇 이름 정하기 (예: "My AI Assistant")
4. 사용자명 정하기 (예: `my_assistant_bot`)
5. BotFather가 **토큰**을 발급해줌

이 토큰을 프로젝트의 `.env` 파일에 넣으면 끝이다.

### 4단계: 실행

```
npm start
```

이 한 줄이면 봇이 시작된다. 텔레그램을 열고 내 봇에게 메시지를 보내면, AI가 답변한다.

```
[INFO] 🚀 Starting Personal Assistant...
[INFO] Copilot client ready (model: sonnet-4.6)
[INFO] ✅ Bot @seyan_personalbot is running!
```

## 뭘 할 수 있나?

텔레그램에서 이런 것들을 할 수 있다:

**💬 일상 대화**
> "다음 주 서울 날씨 어떨 것 같아?" → AI가 웹 검색해서 답변

**📋 할 일 관리**
> "내일까지 발표 자료 준비 추가해줘" → 자동으로 할 일 목록에 저장
> "내 할 일 보여줘" → 저장된 목록 출력

**⏰ 리마인더**
> "오후 3시에 회의 알려줘" → 시간 되면 텔레그램으로 알림

**🎬 유튜브 요약**
> YouTube 링크를 보내면 → 영상 내용을 요약해서 알려줌. 이부분은 전에 내가 만든 [유튜브 요약 도구](https://github.com/sjyang18/youtube-summary)를 다시 사용했다. 이 코트 베이스 (로컬 디렉토리)를 주고, 이것 어떻게 Assistant에서 쓰지? 질문하니 알아서 Configuration을 넣어 줌.

**💻 코드 관련 질문**
> "파이썬으로 CSV 파일 읽는 코드 짜줘" → 코드 생성

**📁 파일 관리**
> "프로젝트 폴더 파일 목록 보여줘" → 원격 머신의 파일 목록 조회

## MCP 서버 — 기존 도구를 그대로 붙이기

이 프로젝트에서 가장 마음에 드는 부분이 **MCP(Model Context Protocol) 서버** 연결이다.

쉽게 말하면, 이미 만들어둔 도구가 있으면 **코드 수정 없이 AI 비서에 바로 연결**할 수 있다. 나는 이전에 만들어둔 **유튜브 요약 도구**를 그대로 가져다 붙였다. 설정 몇 줄 추가하니까 텔레그램에서 "이 영상 요약해줘"가 바로 동작했다.

앞으로 다른 도구들도 같은 방식으로 추가할 계획이다.

## 이전 방식 vs 지금 방식

| | 이전 (Polling + CLI) | 지금 (Copilot SDK) |
|---|---|---|
| AI 통신 | 2초마다 CLI 호출 | 실시간 스트리밍 |
| 기능 | 단순 질의응답 | 10개 도구 + MCP 서버 |
| 대화 기억 | 없음 | 세션별 컨텍스트 유지 |
| 데이터 저장 | 없음 | 할 일/리마인더 영구 저장 |
| 모델 전환 | 불가 | `/model claude-sonnet-4` 한 줄로 |

## 느낀 점

**AI가 AI 앱을 만들어주는 시대**가 왔다.

이번 프로젝트에서 내가 직접 코드를 작성한 건 거의 없다. "조사해줘" → "만들어줘" → "이거 추가해줘" → "에러 고쳐줘" — 이 과정을 Copilot과 대화하면서 진행했을 뿐이다. 나는 **무엇을 만들지** 결정하고, **어떤 도구를 연결할지** 판단하고, **BotFather에서 토큰 발급**한 것 정도가 "내가 한 일"의 전부다.

물론 SDK가 아직 Technical Preview라 가끔 호환성 문제가 있긴 했지만 (예: ESM 모듈 해석 오류), Copilot이 알아서 해결 방법을 찾아줬다.

## 다음 목표

- 업무 도구(Azure DevOps 등)를 MCP로 연결해서, "이번 스프린트 상태 알려줘" 같은 것도 텔레그램에서 가능하게
- 음성 메시지 지원 — 텔레그램 음성 메시지를 텍스트로 변환해서 AI에게 전달

AI와 자동화가 결합되면, 개인 개발자도 **자신만의 AI 비서를 운영하는 시대**가 되었다.
이전 글이 "가능성을 보여주는 프로토타입"이었다면, 이번 글은 "실제로 쓸 수 있는 비서"다.

궁금한 점이 있으면 댓글로 남겨주세요! 🙌
